{
  "name": "aws-secrets-file-writer",
  "version": "0.0.4",
  "type": "module",
  "description": "Reads AWS SecretManager secrets and writes them to files based on the secret key",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:dev": "node --env-file=.env index.js"
  },
  "author": "Robin Edrenius",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.848.0"
  }
}
